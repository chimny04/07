#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int flag = 1;
    int y;
    while(flag != 0)
    {
        // int y;
        y =3;
        flag = 0;
    }
    y = 4; // y is not defined in original code
  
    system("PAUSE");	
    return 0;
}
